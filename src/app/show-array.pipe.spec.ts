import { ShowArrayPipe } from './show-array.pipe';

describe('ShowArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
